export const app = {
    name: '¿ Salut ça taff ?',
    version: '1.0.0',
    repository: 'https://github.com/salut-ca-taff/sct-app'
};

export function title(page) {
    return `${app.name} — ${page}`;
}
